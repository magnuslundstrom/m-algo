import styled from 'styled-components';
import { BaseInput } from './StyledBaseInput';

export const StyledOperationInput = styled(BaseInput)`
  background-color: var(--white);
  border: 1px solid var(--black);
  margin-right: 10px;
`;
