import { SinglyLinkedList, Node } from './singly/singly';

const list = new SinglyLinkedList();
const node = new Node('Hi!');

list.push('a');

export const james = () => {
  return (
    <div>
      <p>How are you!</p>
    </div>
  );
};
